    upstream myapp1 {
        
      {% for item in groups['backends'] -%}
        
         server {{ item }}; 
          
      {% endfor %}
    }

    server {
        
        listen 80 default_server;
        server_name {{ nginx_hostname }};

        location / {
            proxy_pass http://myapp1;
        }
    }
